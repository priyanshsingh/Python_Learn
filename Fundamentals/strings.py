language = "Python"

length = (len(language))
print(length)

# Access Each element of the string

fourth_index = language[4]
print(fourth_index)

multiple_indeces = language[0:3]
print(multiple_indeces)
